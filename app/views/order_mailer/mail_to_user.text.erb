<%= @order.name %>様

この度は、ご注文いただきありがとうございます。

以下のご注文内容に基づき、商品を発送いたします。
到着まで今しばらくお待ちください。

[注文内容]
<%= @order.order_products.each each do |order_product| %>
<%= t("activerecord.attributes.order_product.product_id") %>：<%= order_product.name_and_price_text %>
<%= t("activerecord.attributes.order_product.quantity") %>：<%= order_product.quantity %>
<% end %>
合計金額：<%= number_to_currency(@order.total_price) %>(税込)

<%= t("activerecord.attributes.paymnet_method_id") %>：<%= @order.payment_method_id %>
<%= t("activerecord.attributes.delivery_address") %>：<%= @order.delivery_address %>
<%= t("activerecord.attributes.telephone") %>：<%= @order.telephone %>
<%= t("activerecord.attributes.other_comment") %>：<%= @order.other_comment %>
<%= t("activerecord.attributes.direct_mail_enabled") %>：<%= @order.direct_mail_enabled_text %>

--------------
ゴリラ果物店
メール：support@example.com
--------------
